import React from 'react'

const RecordsView = props => {
  if(props.selectedCompany){
    return (
      <React.Fragment>RecordsView</React.Fragment>
    )
  }
  
  return null
}

export default RecordsView
