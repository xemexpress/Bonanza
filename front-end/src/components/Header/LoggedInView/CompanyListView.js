import React from 'react'

const CompanyListView = props => {
  if(props.selectedCompany){ return null }

  return (
    <React.Fragment>CompanyListView</React.Fragment>
  )
}

export default CompanyListView
